# Enable DXVK GPL Async shader compilation
DXVK_ASYNC=1

# Enable low latency mode (optional, uncomment to use)
# DXVK_FRAME_RATE=0

# Allow CPU frame overlap for low latency (optional)
# DXVK_LOW_LATENCY_ALLOW_CPU_FRAMES_OVERLAP=0
