# DXVK Sarek - Optimized for older GPUs

# Use half CPU cores for shader compilation (default in Sarek)
# Set to 1 to use all cores
DXVK_ALL_CORES=0

# Enable async shader compilation (if using async branch)
# DXVK_ASYNC=1
